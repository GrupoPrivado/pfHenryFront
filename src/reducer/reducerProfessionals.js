const initialState = {
    professionalData:{}
  };
  
  export default function reducerProfessional(state = initialState, action) {
    switch (action.type) {
    
      default:
        return state;
    }
  }
  